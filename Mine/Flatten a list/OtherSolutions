def flat_list(l):

    r = []

    def f(l):

        for i in l:

            r.append(i) if type(i) is int else f(i)

    f(l)

    return r

-------------------------------------------------------------



flat_list=f=lambda d:[d]if int==type(d)else sum(map(f,d),[])

